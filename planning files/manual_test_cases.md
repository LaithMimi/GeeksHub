# Manual Test Cases — SaaS Web Platform

| תרחיש | צעדים / קלט | תוצאה מצופה | סטטוס |
|---|---|---|---|
| התחברות עם משתמש תקין | 1. להיכנס לדף ההתחברות. 2. להזין אימייל: `valid.user@test.com`. 3. להזין סיסמה תקינה: `Test@1234`. 4. ללחוץ על "התחבר". | המשתמש מועבר ל-Dashboard תוך ≤3 שניות, נוצר session פעיל (עוגיית auth קיימת), שם המשתמש מוצג בפינה, ולא מוצגת הודעת שגיאה. | Not Run |
| התחברות עם סיסמה שגויה | 1. להיכנס לדף ההתחברות. 2. להזין אימייל תקין: `valid.user@test.com`. 3. להזין סיסמה שגויה: `Wrong123`. 4. ללחוץ על "התחבר". | מוצגת הודעת שגיאה כללית "אימייל או סיסמה שגויים", המשתמש נשאר בדף ההתחברות, לא נוצר session, ושדה הסיסמה מתאפס. | Not Run |
| התחברות עם שדות ריקים | 1. להיכנס לדף ההתחברות. 2. להשאיר את שדות האימייל והסיסמה ריקים. 3. ללחוץ על "התחבר". | כפתור "התחבר" חסום או מוצגת הודעת ולידציה "שדה חובה" תחת כל שדה ריק, ולא נשלחת בקשה לשרת. | Not Run |
| נעילת חשבון לאחר ניסיונות כושלים | 1. להיכנס לדף ההתחברות. 2. להזין אימייל תקין וסיסמה שגויה. 3. לחזור על שלב 2 חמש פעמים ברצף. | לאחר הניסיון החמישי מוצגת הודעת נעילה/הגבלת קצב, הכניסה נחסמת זמנית, וגם עם סיסמה נכונה לא ניתן להתחבר עד תום זמן ההמתנה. | Not Run |
| התנתקות (Logout) | 1. להתחבר עם משתמש תקין. 2. ללחוץ על תפריט המשתמש. 3. ללחוץ על "התנתק". | המשתמש מועבר לדף ההתחברות, ה-session מבוטל, ולחיצה על "חזור" בדפדפן אינה מחזירה לעמודים מוגנים אלא מפנה להתחברות. | Not Run |
| שחזור סיסמה עם אימייל קיים | 1. להיכנס ל"שכחתי סיסמה". 2. להזין אימייל רשום: `valid.user@test.com`. 3. ללחוץ "שלח קישור". | מוצגת הודעת אישור "נשלח קישור לאיפוס", ובתיבת המייל מתקבל מייל עם קישור איפוס תקף (בתוקף מוגבל) תוך ≤2 דקות. | Not Run |
| שחזור סיסמה עם אימייל לא רשום | 1. להיכנס ל"שכחתי סיסמה". 2. להזין אימייל לא קיים: `noexist@test.com`. 3. ללחוץ "שלח קישור". | מוצגת אותה הודעה גנרית כמו במקרה קיים (מניעת enumeration), ולא נשלח מייל בפועל. | Not Run |
| שימוש חוזר בקישור איפוס פג תוקף | 1. לבקש איפוס סיסמה. 2. להמתין עד תום תוקף הקישור (או להשתמש בקישור שכבר מומש). 3. לפתוח את הקישור. | מוצגת הודעת "הקישור אינו תקף או פג תוקפו", לא ניתן להזין סיסמה חדשה, ומוצע לשלוח קישור מחדש. | Not Run |
| הרשמת משתמש חדש (Happy Path) | 1. להיכנס לדף ההרשמה. 2. להזין אימייל חדש: `new.user@test.com`, שם מלא וסיסמה `Test@1234`. 3. לאשר תנאי שימוש. 4. ללחוץ "הרשמה". | החשבון נוצר, מוצגת הודעת הצלחה, נשלח מייל אימות, והמשתמש מופנה למסך אימות/Dashboard בהתאם לזרימה. | Not Run |
| הרשמה עם אימייל קיים | 1. להיכנס לדף ההרשמה. 2. להזין אימייל שכבר רשום: `valid.user@test.com`. 3. למלא שאר השדות. 4. ללחוץ "הרשמה". | מוצגת הודעת שגיאה "האימייל כבר קיים במערכת", החשבון לא נוצר, והמשתמש נשאר בטופס ההרשמה. | Not Run |
| ולידציית פורמט אימייל | 1. בטופס הרשמה להזין אימייל לא תקין: `user@@test`, `user.com`, `user@ .com`. 2. לצאת מהשדה (blur). | מוצגת הודעת ולידציה "כתובת אימייל אינה תקינה" עבור כל ערך, וכפתור השליחה נשאר חסום. | Not Run |
| אימות אימייל דרך קישור | 1. להירשם עם משתמש חדש. 2. לפתוח את מייל האימות. 3. ללחוץ על קישור האימות. | חשבון המשתמש מסומן כ"מאומת", מוצגת הודעת אישור, וניתן לגשת לפיצ'רים הדורשים אימות אימייל. | Not Run |
| הרשאות — גישת משתמש רגיל לאזור אדמין | 1. להתחבר כמשתמש עם תפקיד `User`. 2. להזין ידנית ב-URL את נתיב האדמין: `/admin`. | מוצגת שגיאת 403/"אין הרשאה" או הפניה ל-Dashboard, ותוכן האדמין אינו נטען כלל. | Not Run |
| הרשאות — פעולות לפי תפקיד | 1. להתחבר כ-`Viewer`. 2. לפתוח רשומה קיימת. 3. לבדוק זמינות כפתורי עריכה/מחיקה. | כפתורי עריכה ומחיקה מוסתרים או מושבתים עבור Viewer, ואילו למשתמש `Editor` הם פעילים. | Not Run |
| הזמנת משתמש חדש (Invitation) | 1. להתחבר כאדמין. 2. לפתוח "הזמנת משתמש". 3. להזין `invitee@test.com` ולבחור תפקיד `Editor`. 4. לשלוח הזמנה. | מוצגת הודעת "הזמנה נשלחה", המשתמש מופיע ברשימה בסטטוס "ממתין", ומייל הזמנה עם קישור הצטרפות מתקבל. | Not Run |
| קבלת הזמנה וסיום הרשמה | 1. לפתוח את מייל ההזמנה. 2. ללחוץ על קישור ההצטרפות. 3. להגדיר סיסמה ולאשר. | המשתמש מצורף ל-tenant המזמין עם התפקיד שהוקצה, הסטטוס משתנה ל"פעיל", ומתאפשרת התחברות. | Not Run |
| Dashboard — טעינת נתונים | 1. להתחבר עם משתמש בעל נתונים קיימים. 2. להמתין לטעינת ה-Dashboard. | כל הווידג'טים/כרטיסי המדדים נטענים עם ערכים אמיתיים (לא placeholder), אין שגיאות בקונסול, וזמן הטעינה ≤3 שניות. | Not Run |
| Dashboard — מצב ריק (Empty State) | 1. להתחבר עם משתמש/tenant ללא נתונים. 2. לצפות ב-Dashboard. | מוצג Empty State מודרך עם הודעה וקריאה לפעולה (למשל "צור פריט ראשון"), ללא ווידג'טים שבורים או ערכי NaN/undefined. | Not Run |
| CRUD — יצירת רשומה | 1. לפתוח מסך רשומות. 2. ללחוץ "חדש". 3. למלא שדות חובה בערכים תקינים. 4. לשמור. | הרשומה נשמרת, מוצגת הודעת הצלחה, הרשומה מופיעה בראש/בתוך הרשימה מיידית, וערכיה תואמים לקלט. | Not Run |
| CRUD — עריכת רשומה | 1. לבחור רשומה קיימת. 2. ללחוץ "ערוך". 3. לשנות שדה (למשל שם). 4. לשמור. | השינוי נשמר ומשתקף ברשימה ובתצוגת הפירוט לאחר רענון, ותאריך "עודכן לאחרונה" מתעדכן. | Not Run |
| CRUD — מחיקת רשומה עם אישור | 1. לבחור רשומה. 2. ללחוץ "מחק". 3. לאשר בדיאלוג האישור. | מוצג דיאלוג אישור לפני המחיקה, לאחר אישור הרשומה נעלמת מהרשימה, מוצגת הודעת הצלחה, והיא אינה מופיעה גם לאחר רענון. | Not Run |
| CRUD — ביטול מחיקה | 1. לבחור רשומה. 2. ללחוץ "מחק". 3. ללחוץ "ביטול" בדיאלוג. | הדיאלוג נסגר, הרשומה נותרת ללא שינוי ברשימה, ולא נשלחת בקשת מחיקה לשרת. | Not Run |
| חיפוש — תוצאה קיימת | 1. לפתוח מסך רשימה. 2. להזין בשדה החיפוש מונח קיים (למשל `Alpha`). | הרשימה מסוננת בזמן אמת ומציגה רק רשומות התואמות למונח, ומספר התוצאות מתעדכן בהתאם. | Not Run |
| חיפוש — ללא תוצאות | 1. להזין בשדה החיפוש מונח שאינו קיים: `zzz999`. | מוצגת הודעת "לא נמצאו תוצאות", הטבלה ריקה ללא שגיאות, וניתן לנקות את החיפוש בלחיצה אחת. | Not Run |
| סינון משולב | 1. לפתוח מסך רשימה. 2. להחיל פילטר סטטוס=`Active` ופילטר תאריך=חודש נוכחי. 3. לאשר. | מוצגות רק רשומות העונות על שני הפילטרים במקביל, מוצג תג פילטר פעיל, ולחיצה על "נקה" מאפסת לתצוגה מלאה. | Not Run |
| ולידציית טופס — שדות חובה | 1. לפתוח טופס יצירה. 2. להשאיר שדה חובה ריק. 3. ללחוץ "שמור". | מוצגת הודעת "שדה חובה" תחת השדה החסר, המיקוד עובר לשדה הראשון השגוי, והטופס אינו נשלח. | Not Run |
| ולידציית טופס — גבולות ערכים | 1. בשדה מספרי להזין ערך שלילי `-5` ובשדה טקסט להזין מעבר למגבלת התווים (למשל 500 תווים בשדה של 255). | מוצגות הודעות ולידציה מתאימות ("ערך חייב להיות חיובי", "חריגה ממספר התווים"), והשמירה נחסמת עד לתיקון. | Not Run |
| העלאת קובץ תקין | 1. לפתוח שדה העלאה. 2. לבחור קובץ `document.pdf` בגודל 2MB. 3. לאשר העלאה. | מוצג פס התקדמות, בסיום מוצגת תצוגה מקדימה/שם הקובץ והודעת הצלחה, והקובץ זמין להורדה לאחר רענון. | Not Run |
| העלאת קובץ בפורמט לא נתמך | 1. לפתוח שדה העלאה. 2. לבחור קובץ `malware.exe`. | ההעלאה נדחית עם הודעה "סוג קובץ אינו נתמך", הקובץ אינו נשמר בשרת, ורשימת הקבצים אינה משתנה. | Not Run |
| העלאת קובץ מעל מגבלת גודל | 1. לפתוח שדה העלאה. 2. לבחור קובץ בגודל 20MB (מעל מגבלת 15MB). | ההעלאה נחסמת עם הודעה "הקובץ חורג מהגודל המרבי (15MB)", ולא מתבצעת קריאת רשת להעלאה. | Not Run |
| התראות — יצירה בזמן אמת | 1. להתחבר בשני משתמשים באותו tenant. 2. ממשתמש A לבצע פעולה המפעילה התראה למשתמש B. | אצל משתמש B מונה ההתראות עולה, ההתראה מופיעה ברשימה עם תוכן וזמן נכונים, ולחיצה עליה מנווטת לפריט הרלוונטי. | Not Run |
| התראות — סימון כנקרא | 1. לפתוח מרכז ההתראות עם התראות שלא נקראו. 2. ללחוץ "סמן הכל כנקרא". | כל ההתראות מסומנות כנקראו, מונה ההתראות מתאפס ל-0, והמצב נשמר לאחר רענון הדף. | Not Run |
| חיוב — שדרוג מנוי | 1. להתחבר כבעל חשבון בתוכנית Free. 2. לפתוח "חיוב/מנוי". 3. לבחור תוכנית `Pro`. 4. להזין כרטיס בדיקה תקין ולאשר. | התשלום מאושר, המנוי משתנה ל-Pro, פיצ'רים נעולים נפתחים מיידית, וחשבונית/קבלה נוצרת בהיסטוריית החיובים. | Not Run |
| חיוב — כרטיס אשראי שנדחה | 1. במסך התשלום להזין כרטיס בדיקה שנדחה (declined). 2. לאשר. | מוצגת הודעת שגיאת תשלום ברורה, המנוי נשאר בתוכנית הקודמת, ולא נגבה חיוב. | Not Run |
| חיוב — ביטול מנוי | 1. במסך המנוי ללחוץ "בטל מנוי". 2. לאשר בדיאלוג. | מוצג אישור ביטול עם תאריך סיום הגישה בפועל, הסטטוס משתנה ל"מבוטל/יפוג בתאריך X", והגישה לפיצ'רי Pro נשמרת עד תום התקופה ששולמה. | Not Run |
| Multi-tenant — בידוד נתונים | 1. להתחבר כמשתמש ב-Tenant A ולתעד מזהה רשומה. 2. להתחבר כמשתמש ב-Tenant B. 3. לנסות לגשת ל-URL של הרשומה מ-Tenant A. | מוצגת שגיאת 403/404, נתוני Tenant A אינם נחשפים למשתמש Tenant B בשום מסך, חיפוש או API. | Not Run |
| Multi-tenant — מעבר בין ארגונים | 1. להתחבר כמשתמש המשויך לשני ארגונים. 2. לבחור בורר הארגון ולעבור מ-Org A ל-Org B. | כל הנתונים (רשימות, Dashboard, הגדרות) מתחלפים להקשר Org B, וההקשר הנבחר נשמר לאחר רענון. | Not Run |
| Session Timeout — פקיעת חוסר פעילות | 1. להתחבר ולהשאיר את המערכת ללא פעילות מעבר לזמן ה-timeout המוגדר. 2. לנסות לבצע פעולה (למשל שמירה). | מוצגת הודעת "ה-session פג, יש להתחבר מחדש", המשתמש מופנה להתחברות, והפעולה לא מתבצעת עד התחברות מחדש. | Not Run |
| Session — ריענון טוקן שקוף | 1. להתחבר ולהישאר פעיל ברציפות מעבר לתוקף ה-access token הקצר. 2. להמשיך לבצע פעולות. | ה-session מתחדש אוטומטית (refresh) ללא ניתוק המשתמש, והפעולות ממשיכות להצליח ללא הפרעה. | Not Run |
| טיפול בשגיאות — נפילת שרת (500) | 1. לבצע פעולה שמדמה תגובת שרת 500 (או ניתוק ה-backend). | מוצגת הודעת שגיאה ידידותית ("אירעה תקלה, נסה שוב"), האפליקציה אינה קורסת/מציגה מסך לבן, ומוצע כפתור "נסה שוב". | Not Run |
| טיפול בשגיאות — ניתוק רשת | 1. לפתוח מסך עם נתונים. 2. לנתק את חיבור הרשת. 3. לנסות לטעון/לשמור נתונים. | מוצגת הודעת מצב לא-מקוון או שגיאת רשת, נתונים שהוזנו אינם אובדים, ובחיבור מחדש ניתן להשלים את הפעולה. | Not Run |
| Responsive — תצוגת מובייל | 1. לפתוח את הפלטפורמה ברוחב 375px (iPhone) דרך DevTools. 2. לנווט בין מסכים מרכזיים. | הפריסה מסתגלת לעמודה אחת, התפריט הופך ל-hamburger, אין גלילה אופקית, וכל הכפתורים לחיצים ללא חפיפה. | Not Run |
| Responsive — טאבלט ורוחב ביניים | 1. לפתוח את הפלטפורמה ברוחב 768px ו-1024px. 2. לבדוק טבלאות וטפסים. | הרכיבים מסתדרים לרשת מתאימה, טבלאות ניתנות לגלילה/קריאה, ואין חיתוך של טקסט או אלמנטים חופפים. | Not Run |
| תאימות דפדפנים | 1. לבצע זרימת התחברות → יצירת רשומה → התנתקות בכל אחד מ: Chrome, Firefox, Edge, Safari (גרסה עדכנית). | הזרימה מסתיימת בהצלחה בכל הדפדפנים, העיצוב עקבי, ואין שגיאות JS בקונסול בכל אחד מהם. | Not Run |
| אבטחה — הגנת XSS בשדות קלט | 1. בשדה טקסט (למשל שם רשומה) להזין `<script>alert('xss')</script>`. 2. לשמור ולצפות ברשומה. | הקלט מוצג כטקסט מנוטרל ולא מורץ כקוד, לא מופיע חלון alert, והערך נשמר בצורה מקודדת (encoded). | Not Run |
| אבטחה — גישה ישירה ל-URL מוגן ללא התחברות | 1. בלי להתחבר, להדביק בדפדפן URL של דף מוגן: `/dashboard`. | המשתמש מופנה לדף ההתחברות, תוכן מוגן אינו נטען, ולאחר התחברות מוצלחת ניתן להפנות חזרה ליעד המקורי. | Not Run |
| אבטחה — חשיפת נתונים רגישים | 1. לפתוח DevTools → Network. 2. לבצע התחברות ולצפות בתגובות ה-API. | הסיסמה אינה מוחזרת בתגובה, טוקנים מאוחסנים בצורה מאובטחת (HttpOnly cookie), ואין הדלפת נתוני משתמשים אחרים בתגובות. | Not Run |

**סה"כ: 45 תרחישי בדיקה** המכסים happy path, תרחישים שליליים ו-edge cases בכל התחומים שהוגדרו.
